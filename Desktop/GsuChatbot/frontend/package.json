{
  "name": "gsu-chatbot-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "axios": "^1.6.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "fix-schema-utils": "mkdir -p node_modules/babel-loader/node_modules && (cd node_modules/babel-loader/node_modules && npm install schema-utils@2.7.1 --no-save --legacy-peer-deps 2>/dev/null || true) && (cd node_modules && npm install ajv-keywords@3.5.2 --no-save --legacy-peer-deps 2>/dev/null || true) || true"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "ajv-keywords": "^3.5.2"
  }
}
